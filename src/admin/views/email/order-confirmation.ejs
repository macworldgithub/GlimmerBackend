<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Order Confirmation</title>
  <style type="text/css">
    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      background-color: #f4f4f7;
      margin: 0;
      padding: 0;
      color: #51545E;
    }
    .email-wrapper {
      width: 100%;
      background-color: #f4f4f7;
      padding: 20px;
    }
    .email-content {
      max-width: 600px;
      margin: 0 auto;
      background-color: #FFFFFF;
      border-radius: 6px;
      overflow: hidden;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
    }
    .email-header {
      background-color: #FFF8E1;
      padding: 30px 20px;
      text-align: center;
      color: #FFFFFF;
    }
    .email-header img.logo {
      max-width: 140px;
      margin-bottom: 10px;
    }
    .email-body {
      padding: 30px 20px;
    }
    h1 {
      margin: 0 0 10px;
      font-size: 24px;
      font-weight: 700;
      color: #000000;
    }
    p {
      font-size: 15px;
      line-height: 1.6;
      margin: 0 0 10px;
      color: #000000;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      font-size: 14px;
    }
    th, td {
      padding: 12px 15px;
      text-align: left;
      border-bottom: 1px solid #EAEAEC;
    }
    th {
      background-color: #f9fafc;
      font-weight: 600;
    }
    .product-img {
      width: 50px;
      height: auto;
      vertical-align: middle;
      border-radius: 4px;
      margin-right: 10px;
    }
    .footer {
      padding: 25px 20px;
      text-align: center;
      font-size: 12px;
      color: #A8AAAF;
      background-color: #f9fafc;
    }
    .cta-button {
      display: inline-block;
      margin-top: 25px;
      background-color: #8212eb;
      color: #ffffff !important;
      padding: 12px 24px;
      border-radius: 4px;
      text-decoration: none;
      font-weight: bold;
      font-size: 15px;
    }
    .totals {
      font-weight: bold;
      text-align: right;
    }
    .totals td {
      border: none;
      padding-top: 10px;
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="email-content">

      <!-- Header -->
      <div class="email-header">
        <!-- Logo -->
        <img
          src="https://via.placeholder.com/150x50?text=Glimmer+Logo"
          alt="Glimmer Logo"
          class="logo"
        />
        <h1>Thank you for your order, <%= customer.name %>!</h1>
        <p>Order #<%= order.id %> &bull; <%= order.date %></p>
      </div>

      <!-- Body -->
      <div class="email-body">
        <p>Hi <%= customer.name %>,</p>
        <p>We’re excited to let you know we’ve received your order. Here’s a summary:</p>

        <table>
          <thead>
            <tr>
              <th>Item</th>
              <th>Qty</th>
              <th>Price</th>
              <th>Total</th>
            </tr>
          </thead>
          <tbody>
            <% order.items.forEach(item => { %>
              <tr>
                <td>
                  <img src="<%= item.image %>" alt="<%= item.name %>" class="product-img" />
                  <%= item.name %>
                </td>
                <td><%= item.quantity %></td>
                <td>PKR <%= order.total.toFixed(2) %></td>
                <td>PKR <%= order.subtotal.toFixed(2) %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>

        <table class="totals" width="100%">
          <tr>
            <td>Price:</td>
            <td>PKR <%= order.total.toFixed(2) %></td>
          </tr>
          <tr>
            <td>Shipping:</td>
            <td>PKR 200</td>
          </tr>
          <tr style="font-size: 16px;">
            <td>Total:</td>
            <td>PKR <%= order.subtotal.toFixed(2) %></td>
          </tr>
        </table>

        <a href="https://glimmer.com.pk" class="cta-button">Visit Our Store</a>

        <p style="margin-top: 25px;">If you have any questions, reply to this email or contact our support team at <a href="mailto:support@glimmer.com.pk">support@glimmer.com.pk</a>.</p>
        <p>Thanks again for choosing Glimmer!</p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>© <%= new Date().getFullYear() %> Glimmer. All rights reserved.</p>
      </div>

    </div>
  </div>
</body>
</html>

